---
// src/pages/marketing.astro
import Layout from '../layouts/Layout.astro';
import marketingImg from '../assets/marketing.webp';
import marketingImg1 from '../assets/marketing1.webp';
import marketingImg2 from '../assets/marketing2.webp';
import marketingImg3 from '../assets/marketing3.webp';
import marketingImg4 from '../assets/marketing4.webp';
import marketingImg5 from '../assets/marketing5.webp';

---

<Layout 
  title="Marketing Services | JLO Global Resources" 
  description="Data-driven marketing strategies tailored to your goals. We help you reach your target audience and drive measurable growth."
>
  
  {/* Hero Section */}
  <section class="bg-[#00664e] text-white page-hero relative overflow-hidden">
    <div class="container mx-auto px-4 relative z-10 text-center">
      <h1 class="text-4xl md:text-6xl font-extrabold mb-6 tracking-tight">
        Marketing Services
      </h1>
      <p class="text-xl md:text-2xl text-gray-100 max-w-3xl mx-auto leading-relaxed">
        Data-driven marketing strategies tailored to your goals.
      </p>
    </div>
  </section>

  {/* Main Content Section */}
  <section class="py-20 bg-gradient-to-b from-slate-50 to-white">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
        
        {/* Image Side */}
        <div class="order-2 md:order-1">
          <div class="relative rounded-2xl overflow-hidden shadow-2xl">
            <img 
              src={marketingImg.src} 
              alt="Marketing Services" 
              loading="eager"
              width="600"
              height="500"
              class="w-full aspect-[8/9] md:aspect-[3/2] object-cover"
            />
          </div>
        </div>

        {/* Text Side */}
        <div class="order-1 md:order-2 space-y-6">
          <span class="text-[#0d55a9] font-bold tracking-widest uppercase text-sm">
            Growth Strategy
          </span>
          <h2 class="text-3xl md:text-5xl font-bold text-slate-900 leading-tight">
            Comprehensive Marketing Solutions
          </h2>
          <p class="text-slate-600 text-lg leading-relaxed">
            We help you reach your target audience and drive measurable growth through data-driven marketing strategies. Our comprehensive approach combines creativity with analytics to deliver results that matter.
          </p>
          <p class="text-slate-600 text-lg leading-relaxed">
            From digital marketing and social media management to brand development and campaign execution, we provide end-to-end marketing services tailored to your business objectives.
          </p>
        </div>

      </div>
    </div>
  </section>

  {/* Photo Carousel Section */}
  <section class="py-20 bg-gradient-to-b from-white via-slate-50 to-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-slate-900 mb-4">
          Our Marketing Success Stories
        </h2>
        <p class="text-slate-600 text-lg max-w-2xl mx-auto">
          Showcasing our successful campaigns and marketing projects
        </p>
      </div>

      <div class="relative">
        <div id="marketing-carousel" class="overflow-hidden rounded-2xl">
          {/* Carousel now contains 5 images instead of 6 */}
          <div class="flex transition-transform duration-700 ease-in-out" style="transform: translateX(0%)">
            <div class="min-w-full md:min-w-[50%] lg:min-w-[33.333%] px-3">
              <div class="relative group rounded-xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-500 aspect-[8/9]">
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>
                <img 
                  src={marketingImg1.src} 
                  alt="Marketing Services Photo 1" 
                  loading="lazy"
                  width="400"
                  height="450"
                  class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
                />
                <div class="absolute bottom-0 left-0 right-0 p-6 transform translate-y-full group-hover:translate-y-0 transition-transform duration-500 z-20">
                  <p class="text-white font-semibold text-lg">Digital Campaigns</p>
                </div>
              </div>
            </div>
            <div class="min-w-full md:min-w-[50%] lg:min-w-[33.333%] px-3">
              <div class="relative group rounded-xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-500 aspect-[8/9]">
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>
                <img 
                  src={marketingImg2.src} 
                  alt="Marketing Services Photo 2" 
                  loading="lazy"
                  width="400"
                  height="450"
                  class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
                />
                <div class="absolute bottom-0 left-0 right-0 p-6 transform translate-y-full group-hover:translate-y-0 transition-transform duration-500 z-20">
                  <p class="text-white font-semibold text-lg">Brand Development</p>
                </div>
              </div>
            </div>
            <div class="min-w-full md:min-w-[50%] lg:min-w-[33.333%] px-3">
              <div class="relative group rounded-xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-500 aspect-[8/9]">
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>
                <img 
                  src={marketingImg3.src} 
                  alt="Marketing Services Photo 3" 
                  loading="lazy"
                  width="400"
                  height="450"
                  class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
                />
                <div class="absolute bottom-0 left-0 right-0 p-6 transform translate-y-full group-hover:translate-y-0 transition-transform duration-500 z-20">
                  <p class="text-white font-semibold text-lg">Social Media</p>
                </div>
              </div>
            </div>
            <div class="min-w-full md:min-w-[50%] lg:min-w-[33.333%] px-3">
              <div class="relative group rounded-xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-500 aspect-[8/9]">
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>
                <img 
                  src={marketingImg4.src} 
                  alt="Marketing Services Photo 4" 
                  loading="lazy"
                  width="400"
                  height="450"
                  class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
                />
                <div class="absolute bottom-0 left-0 right-0 p-6 transform translate-y-full group-hover:translate-y-0 transition-transform duration-500 z-20">
                  <p class="text-white font-semibold text-lg">Content Strategy</p>
                </div>
              </div>
            </div>
            <div class="min-w-full md:min-w-[50%] lg:min-w-[33.333%] px-3">
              <div class="relative group rounded-xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-500 aspect-[8/9]">
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>
                <img 
                  src={marketingImg5.src} 
                  alt="Marketing Services Photo 5" 
                  loading="lazy"
                  width="400"
                  height="450"
                  class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
                />
                <div class="absolute bottom-0 left-0 right-0 p-6 transform translate-y-full group-hover:translate-y-0 transition-transform duration-500 z-20">
                  <p class="text-white font-semibold text-lg">SEO & SEM</p>
                </div>
              </div>
            </div>
            {/* 6th Image Removed */}
          </div>
        </div>

        <button 
          id="marketing-prev"
          class="absolute left-4 top-1/2 -translate-y-1/2 bg-white/90 hover:bg-white text-slate-900 p-3 rounded-full shadow-lg hover:shadow-xl transition-all duration-300 z-30"
          aria-label="Previous slide"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </button>
        <button 
          id="marketing-next"
          class="absolute right-4 top-1/2 -translate-y-1/2 bg-white/90 hover:bg-white text-slate-900 p-3 rounded-full shadow-lg hover:shadow-xl transition-all duration-300 z-30"
          aria-label="Next slide"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </button>
      </div>

      <div class="flex justify-center gap-2 mt-8">
        <button class="carousel-dot w-2 h-2 rounded-full bg-[#00664e] transition-all duration-300" data-slide="0"></button>
        <button class="carousel-dot w-2 h-2 rounded-full bg-gray-300 hover:bg-gray-400 transition-all duration-300" data-slide="1"></button>
        <button class="carousel-dot w-2 h-2 rounded-full bg-gray-300 hover:bg-gray-400 transition-all duration-300" data-slide="2"></button>
        {/* Removed 4th dot because with 5 images on desktop (3 visible), we only need 3 index positions */}
      </div>
    </div>
  </section>

  {/* Removed Featured Campaign & Testimonials Section */}

  <script>
    (function() {
      const carousel = document.getElementById('marketing-carousel');
      const prevBtn = document.getElementById('marketing-prev');
      const nextBtn = document.getElementById('marketing-next');
      const dots = document.querySelectorAll('#marketing-carousel ~ div ~ div .carousel-dot');
      const carouselInner = carousel?.querySelector('.flex') as HTMLElement;
      let currentSlide = 0;
      const totalImages = 5; // Updated to 5
      
      // @ts-nocheck
      function getSlidesPerView() {
        const width = window.innerWidth;
        if (width >= 1024) return 3; // lg: 3 slides
        if (width >= 768) return 2;  // md: 2 slides
        return 1; // mobile: 1 slide
      }
      
      // @ts-nocheck
      function getMaxSlides() {
        const slidesPerView = getSlidesPerView();
        return Math.max(1, totalImages - slidesPerView + 1);
      }
      
      // @ts-nocheck
      function updateCarousel() {
        const slidesPerView = getSlidesPerView();
        const slideWidth = 100 / slidesPerView;
        const translateX = -(currentSlide * slideWidth);
        
        if (carouselInner) {
          carouselInner.style.transform = `translateX(${translateX}%)`;
        }
        
        // @ts-nocheck 
        dots.forEach((dot, index) => {
          if (index === currentSlide) {
            dot.classList.remove('bg-gray-300');
            dot.classList.add('bg-[#00664e]');
            dot.classList.remove('w-2');
            dot.classList.add('w-8');
          } else {
            dot.classList.add('bg-gray-300');
            dot.classList.remove('bg-[#00664e]');
            dot.classList.add('w-2');
            dot.classList.remove('w-8');
          }
        });
      }

      nextBtn?.addEventListener('click', () => {
        const maxSlides = getMaxSlides();
        currentSlide = (currentSlide + 1) % maxSlides;
        updateCarousel();
      });

      prevBtn?.addEventListener('click', () => {
        const maxSlides = getMaxSlides();
        currentSlide = (currentSlide - 1 + maxSlides) % maxSlides;
        updateCarousel();
      });

      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          currentSlide = index;
          updateCarousel();
        });
      });

      // Handle window resize
      let resizeTimeout: ReturnType<typeof setTimeout>;
      window.addEventListener('resize', () => {
        clearTimeout(resizeTimeout);
        resizeTimeout = setTimeout(() => {
          const maxSlides = getMaxSlides();
          if (currentSlide >= maxSlides) {
            currentSlide = maxSlides - 1;
          }
          updateCarousel();
        }, 250);
      });

      setInterval(() => {
        const maxSlides = getMaxSlides();
        currentSlide = (currentSlide + 1) % maxSlides;
        updateCarousel();
      }, 5000);
    })();
  </script>

  {/* Services Offered Section */}
  <section class="py-20 bg-slate-50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-slate-900 mb-4">
          Our Marketing Services
        </h2>
        <p class="text-slate-600 text-lg max-w-2xl mx-auto">
          Full-spectrum marketing solutions to grow your business
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="bg-white p-8 rounded-xl shadow-lg">
          <h3 class="text-xl font-bold text-slate-900 mb-3">Digital Marketing</h3>
          <p class="text-slate-600">
            SEO, SEM, social media advertising, and content marketing to boost your online presence.
          </p>
        </div>
        <div class="bg-white p-8 rounded-xl shadow-lg">
          <h3 class="text-xl font-bold text-slate-900 mb-3">Brand Development</h3>
          <p class="text-slate-600">
            Strategic branding and identity development to establish a strong market presence.
          </p>
        </div>
        <div class="bg-white p-8 rounded-xl shadow-lg">
          <h3 class="text-xl font-bold text-slate-900 mb-3">Campaign Management</h3>
          <p class="text-slate-600">
            End-to-end campaign planning, execution, and performance tracking for optimal ROI.
          </p>
        </div>
      </div>
    </div>
  </section>

  {/* CTA Section */}
  <section class="bg-[#00221a] text-white py-20 text-center">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl md:text-4xl font-bold mb-6">Ready to Grow Your Business?</h2>
      <p class="text-gray-300 mb-8 max-w-2xl mx-auto">
        Let's discuss your marketing goals and create a strategy that drives real results.
      </p>
      <a href="/contact" class="btn-gradient inline-block px-10 py-4 rounded-full font-bold text-lg tracking-wide shadow-lg hover:shadow-xl">
        Get Started
      </a>
    </div>
  </section>

</Layout>